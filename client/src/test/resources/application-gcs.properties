rawdata.topic=test-topic

rawdata.client.provider=gcs

gcs.bucket-name=REPLACE_WITH_GCS_BUCKET_NAME

# Service Account
gcs.credential-provider=service-account
gcs.service-account.key-file=REPLACE_WITH_FULL_FILE_PATH_TO_SERVICE_ACCOUNT_KEY.json

# Workload Identity Service Account (compute engine - cloud managed)
#gcs.credential-provider=compute-engine
#gcs.service-account.key-file=

gcs.listing.min-interval-seconds=30

local-temp-folder=target/avro/temp
avro-file.max.seconds=86400
avro-file.max.bytes=67108864
avro-file.sync.interval=524288
